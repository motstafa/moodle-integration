# Laravel backend base URL, no trailing slash, including /api path
NEXT_PUBLIC_API_URL=http://localhost:8000/api
# Must match REFRESH_SECRET in backend/.env — never commit the real value
NEXT_PUBLIC_REFRESH_TOKEN=
